# Vagrant Box for various uses

## PHP

If you want to run this with PHP make sure you run the following commands:

`vagrant provision --provision-with Apache`
`vagrant provision --provision-with PHP`